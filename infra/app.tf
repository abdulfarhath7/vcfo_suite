# App Runner runs the Next.js container from ECR. Autoscaling, no servers.
# TODO: ECR repo, IAM role (least privilege: this bucket + secrets only),
# App Runner service pointing DATABASE_URL/S3_* at RDS + S3 above.
# Stub kept intentionally minimal — flesh out in Stage 2.
