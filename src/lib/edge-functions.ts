/**
 * Edge-function registry retired — those flows are normal App Router API routes now.
 * Kept as an empty module so any lingering import path resolves during cleanup.
 */
export const EDGE_FUNCTION_PATHS = {} as const;
export const edgeFunctionRegistry = {} as const;
